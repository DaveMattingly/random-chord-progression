{
  "dependencies": {
    "better-sqlite3": "^12.2.0",
    "cors": "^2.8.5",
    "csv-parser": "^3.2.0",
    "express": "^5.1.0",
    "seedrandom": "^3.0.5",
    "strip-bom-stream": "^5.0.0",
    "tonal": "^6.4.2",
    "workspace": "github:tonaljs/tonal"
  },
  "scripts": {
    "install": "cd client && npm install && cd ../node && npm install",
    "build": "cd client && npm run build",
    "start": "node node/server.js"
  }
}
